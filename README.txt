Multilingual POS Tagger — Streamlit App
--------------------------------------
1. Create venv:
   python -m venv venv
   .\venv\Scripts\Activate.ps1
2. Install requirements:
   pip install -r requirements.txt
3. Run app:
   streamlit run app.py
